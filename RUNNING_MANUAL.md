# كتيب التشغيل وضبط البيئة البرمجية (App's Operations & Running Manual) 🛠️📱
### **Compilation, Executing Local Tests, Custom Branding, and Store Manifest Guidelines**

يختص هذا الدليل بمهندسي البرمجيات ومدراء المشاريع لشرح كيفية بناء المشروع، تتبع وبناء واجهات التطبيق، إطلاق الاختبارات للتحقق، وإدارة وتحديث الأيقونات والهوية التجارية الفاخرة للتطبيق.

---

## 1. 📋 متطلبات بيئة التطوير والتشغيل الفنية (Engine Prerequisites)
لبناء وتشغيل هذا المشروع محلياً أو على خوادم البناء المؤتمتة (CI/CD)، يرجى التأكد من استيفاء المتطلبات التالية:
* **نظام التشغيل ومحيط التطوير:** نظام Android Studio (إصدار Ladybug 2024.2.1 أو أحدث).
* **نسخة لغة الجافا (JDK):** توافق كامل مع **JDK 17** المعتمد كمقیاس رسمي لمشاريع أندرويد الحديثة.
* **إصدار Kotlin:** تعتمد لغة تطوير التطبيق على Kotlin إصدار `1.9.x` أو أعلى.
* **حدود حزم دعم الهاتف (SDK Bounds):**
  * **الحد الأدنى لقابلية التشغيل (`minSdk`):** `26` (يتوافق مع Android 8.0 Oreo وما فوق، وهي تغطي أكثر من 95% من الهواتف النشطة عالمياً).
  * **الحد المستهدف للتشغيل والترقية للمتاجر (`targetSdk / compileSdk`):** `34` (متوافق مع أحدث معايير الأمان المعتمدة في متجر Google Play).

---

## 2. 🧪 اختبار جودة وحدات منطق الأعمال الحاكمة (Local Tests Execution)
يحتوي المشروع على ملف اختبارات وحدات برمجية شامل ومؤتمت بالكامل للتحقق من سلامة كافة تفاصيل السلوك التجاري وقاعدة البيانات المتقاطعة للولاء (`LoyaltyDomainTest.kt`) عبر منصة **Robolectric** لمحاكاة بيئة أندرويد السريعة على الـ JVM دون الحاجة لمحاكي حقيقي ممتص للأداء والموارد.

### أ. كيفية تشغيل الاختبارات البرمجية محلياً:
يمكن تشغيل الاختبارات للتثبت من خلو التطبيق من الأخطاء التراكمية في أي وقت من خلال موجّـه الأوامر في مجلد المشروع الرئيسي بالطلب التالي:
```bash
gradle :app:testDebugUnitTest
```

### ب. ماذا يختبر هذا النظام المؤتمت بالضبط؟
* **تداخل الكوبونات ومكافحة الاحتيال:** يتحقق من منع المسح المتكرر للمستخدم لبطاقة لم يتم استردادها مسبقاً.
* **إمكانية إعادة المسح بعد الاستهلاك:** يثبت تفعيل مطالبة الباقة الجديدة فور تصفير واسترداد القسيمة القديمة بنجاح.
* **الترقية والدفع للمحفظة:** يتثبت من انتقال الحالات بنجاح إلى `"added_to_wallet"`.
* **مبدأ تدوير طوابع الولاء ومكافآتها الكبرى:** يتحقق من تراكم النقاط بمقدار 100 مع كل طابع، وإعادة تصفير العداد إلى الطابع الأول بنجاح وتوزيع 250 نقطة مكافأة بونص تلقائية ومجانية بمجرد استهلاك الطابع الخامس في العداد الحاكم.
* **أقسام وتاريخ المسح لملفات السياج الجغرافي:** واختبار أمان تفريغ ومحو المسارات بالكامل عند طلب المستخدم النهائي لحماية خصوصيته.

---

## 3. 📦 دورة بناء التطبيق واستخراج ملف الـ APK النهائي للتحميل
للحصول على الحزمة البرمجية النهائية للتثبيت الفوري على أجهزة المستخدمين للاختبار الميداني أو للرفع إلى خدمات المتاجر (Google Play Console):

### أ. إصدار البناء الاختباري (Generate Debug APK)
لبناء حزمة أندرويد التجريبية الفورية بسرعة وسلاسة:
```bash
gradle assembleDebug
```
* **مكان توليد ملف البناء:** ستجد الحزمة الجاهزة للتنزيل والنقل اليدوي للهواتف في المسار التالي داخل المشروع:
  `app/build/outputs/apk/debug/app-debug.apk`

### ب. إصدار الرفع الرسمي للمتاجر (Generate Release App Bundle - AAB)
لبناء صيغة الرفع الأمثل المعتمدة والمشفرة من متجر قوقل بلاي والتي تختزل وتوفر مساحة الأجهزة للمستخدمين النهائيين:
```bash
gradle bundleRelease
```
* **مكان توليد ملف البناء الرفع الكلي:** ستجده في المسار التالي جاهزاً للرفع مجهّزاً بتوقيعك الأمني التجاري:
  `app/build/outputs/bundle/release/app-release.aab`

---

## 4. 🎨 إعداد وإدارة الهوية البصرية والأيقونات الفاخرة (App Adaptive Icons)
تم إعداد وضبط وتحديث كامل الهوية البصرية للأيقونات التكيفية الفاخرة المعتمدة لدى نظام أندرويد (Adaptive Icons) لتبدو عصرية وفخمة للغاية مع خلفيات داكنة معدنية ولمسات ورموز ذهبية عريقة:

### أ. هيكل بناء رمز التطبيق (Adaptive Icon Resource Trees)
* **الصيغة الشعاعية للخلفية (Background Path):** تم إعداد الكود الشعاعي المرن للتدرجات اللونية الفاخرة في المسار التالي:
  `app/src/main/res/mipmap-anydpi-v26/ic_launcher_background.xml`
* **الطبقة الأمامية التفاعلية (Foreground Visual Layer):** تدمج الرمز البصري الذهبي المصمم خصيصاً على جهاز المستخدم في المسار التالي:
  `app/src/main/res/drawable/ic_launcher_foreground.xml`

يضمن هذا التكامل ظهور أيقونة التطبيق بشكل ديناميكي وجاذب وثابت، وقابليتها للتحول والتوافق مع أي سمات لهواتف المستخدمين بمرونة بالغة.
